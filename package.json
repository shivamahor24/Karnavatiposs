{
  "name": "anndevta-pos-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root workspace workspace runner for Anndevta POS",
  "scripts": {
    "dev": "npm --prefix frontend run electron:dev",
    "start": "npm --prefix frontend run start",
    "build": "npm --prefix frontend run build",
    "electron:dev": "npm --prefix frontend run electron:dev",
    "electron:build": "npm --prefix frontend run electron:build",
    "electron:pack": "npm --prefix frontend run electron:pack"
  }
}
